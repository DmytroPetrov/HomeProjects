swagger: "2.0"
host: “myhost.com”
consumes:
  - application/json
  - application/text
produces:
  - application/json
  - application/text
securityDefinitions:
  Cookie:
    description: "Play cookie-based authentication"
    type: apiKey,
    in: header,
    name: Cookie
